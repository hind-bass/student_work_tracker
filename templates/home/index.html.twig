{# templates/home/index.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestionnaire de Travaux Universitaires</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .hero-section {
            color: white;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 15px;
            padding: 2rem;
            color: white;
            transition: all 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.15);
        }

        .btn-custom {
            padding: 12px 40px;
            font-size: 1.1rem;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>
<div class="container">
    {# Hero Section #}
    <div class="row justify-content-center text-center mb-5">
        <div class="col-lg-10">
            <div class="hero-section">
                <i class="bi bi-journal-bookmark-fill" style="font-size: 5rem; margin-bottom: 1rem;"></i>
                <h1 class="display-3 fw-bold mb-4">Gestionnaire de Travaux Universitaires</h1>
                <p class="lead mb-5" style="font-size: 1.4rem;">
                    Organisez vos devoirs, gérez vos échéances et suivez votre progression en toute simplicité.
                </p>

                <div class="d-flex gap-3 justify-content-center flex-wrap">
                    <a href="{{ path('app_register') }}" class="btn btn-light btn-custom">
                        <i class="bi bi-person-plus me-2"></i> Créer un compte
                    </a>
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light btn-custom">
                        <i class="bi bi-box-arrow-in-right me-2"></i> Se connecter
                    </a>
                </div>
            </div>
        </div>
    </div>

    {# Features #}
    <div class="row g-4 mt-5">
        <div class="col-md-4">
            <div class="feature-card text-center">
                <i class="bi bi-card-checklist" style="font-size: 3rem; margin-bottom: 1rem;"></i>
                <h4 class="mb-3">Gestion des Travaux</h4>
                <p>Centralisez tous vos devoirs et projets en un seul endroit avec un suivi complet.</p>
            </div>
        </div>

        <div class="col-md-4">
            <div class="feature-card text-center">
                <i class="bi bi-calendar-event" style="font-size: 3rem; margin-bottom: 1rem;"></i>
                <h4 class="mb-3">Suivi des Échéances</h4>
                <p>Ne manquez plus jamais une deadline avec notre système d'alertes intelligent.</p>
            </div>
        </div>

        <div class="col-md-4">
            <div class="feature-card text-center">
                <i class="bi bi-graph-up" style="font-size: 3rem; margin-bottom: 1rem;"></i>
                <h4 class="mb-3">Statistiques</h4>
                <p>Visualisez votre progression et vos performances avec des graphiques clairs.</p>
            </div>
        </div>
    </div>

    {# Stats #}
    <div class="row mt-5 text-center">
        <div class="col-md-4">
            <div class="feature-card">
                <h2 class="display-4 fw-bold mb-2">100%</h2>
                <p class="mb-0">Gratuit</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="feature-card">
                <h2 class="display-4 fw-bold mb-2">24/7</h2>
                <p class="mb-0">Accessible</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="feature-card">
                <h2 class="display-4 fw-bold mb-2">∞</h2>
                <p class="mb-0">Travaux</p>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
